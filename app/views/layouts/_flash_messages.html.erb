<% unless flash.empty? %>
  <% flash.each do |type, message| %>
    <div class="row">
      <div class="col-12">
        <div id="flash_<%= flash_class(type) %>"
             class="<%= flash_class(type) %>">
          <button class="close" data-dismiss="alert">×</button>
          <%= message %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>